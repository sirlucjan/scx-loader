[workspace]
members = [
    "crates/scx_loader",
    "crates/scxctl",
]
resolver = "2"

[profile.release]
lto = "thin"

[profile.release-tiny]
inherits = "release"
lto = "thin"
strip = true
incremental = true

[profile.release-fast]
inherits = "release"
lto = false
incremental = true
